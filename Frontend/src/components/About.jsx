import React, { useContext } from "react";
import { NoteContext } from '../context/notes/NoteContext';

const About = () => {
  // Using the context
  // const a = useContext(NoteContext);
  
  return (
    <>
      <h1>This is About: Umair Ahmed i am studying in Section B </h1>
    </>
  );
};

export default About;
